<div class="col-md-6 cl-md-offset-3"></div>
	<h3><%= @item.title %></h3>
	<p><%= @item.description %></p>

	<%= link_to "Back", root_path, class: "btn btn-success" %>
	<%= link_to "Edit", edit_item_path(@item), class: "btn btn-primary"%>
</div>